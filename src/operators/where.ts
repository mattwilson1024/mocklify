// import { Limiter } from '../limiter';
// import { applyOperators, Operator } from '../operator';

// export function where<T>(limiter: Limiter<T>, operators: Operator<T>[]): Operator<T> {
//   return (items: T[]): T[] => {
//     return applyOperators(items, operators, limiter);
//   }
// }
